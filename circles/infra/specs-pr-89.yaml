# specs-89.circles.teststuff.net → this circles PR's specs-site preview (bucket
# circles-specs, prefix pr-89/). Generated by circles scripts/specs-pr-route.sh; removed
# when PR #89 closes. See circles/infra/README.md.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: specs-pr-89
  namespace: circles
spec:
  parentRefs:
    - name: circles-gateway
  hostnames:
    - specs-89.circles.teststuff.net
  rules:
    - matches:
        - path: { type: PathPrefix, value: / }
      filters:
        - type: URLRewrite
          urlRewrite:
            hostname: circles-specs.teststuff.net
            path: { type: ReplacePrefixMatch, replacePrefixMatch: /pr-89 }
      backendRefs:
        - group: ""
          kind: Service
          name: garage-s3
          namespace: garage
          port: 3902
